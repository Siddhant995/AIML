smart_data_predictor_django - Django port of the Smart Data Predictor app.

Quick start:
  - pip install -r requirements.txt
  - python manage.py migrate
  - python manage.py runserver

Or with Docker:
  - docker-compose up --build
  - open http://localhost:8000
